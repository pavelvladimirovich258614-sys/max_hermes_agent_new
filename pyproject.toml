[project]
name = "max-hermes-agent"
version = "1.0.0"
description = "MAX Messenger channel plugin for Hermes Agent Gateway"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"

[project.urls]
Repository = "https://github.com/pavelvladimirovich258614-sys/max_hermes_agent_new"

[tool.pytest.ini_options]
testpaths = ["plugin/max/tests"]
